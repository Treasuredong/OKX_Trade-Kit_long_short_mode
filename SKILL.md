---
name: btc-usdt-swap-defensive-ai
description: 面向日收益优化的扩展多标的 AI 永续合约策略 Skill。交易 BTC、ETH、SOL、XRP、OKB、ORDI、TRUMP、DOGE、BNB、RAVE 的 USDT 永续合约，使用无人值守主动扫描、1H 主执行、4H 趋势过滤、深度波段限价委托、持仓后自动管理、资金异动 + OI 四象限 + News 情绪雷达 + 大户动向代理信号 + 风险偏好代理 + 多币种相关性联合评分、动态杠杆、下单即附带止盈止损，通过 Agent Trade Kit 自动执行。
---

# 策略名称
多标的无人值守日收益波段 AI 策略 V4.1

# 适用范围
本 Skill 只允许交易以下 `USDT` 永续合约：

- `BTC-USDT-SWAP`
- `ETH-USDT-SWAP`
- `SOL-USDT-SWAP`
- `XRP-USDT-SWAP`
- `OKB-USDT-SWAP`
- `ORDI-USDT-SWAP`
- `TRUMP-USDT-SWAP`
- `DOGE-USDT-SWAP`
- `BNB-USDT-SWAP`
- `RAVE-USDT-SWAP`

本版本的核心目标是：不再关注排行榜统计，而是围绕账户净值稳定增长，尽量让每天都有正收益。策略不追求高频小钱，而是用更深的波段委托、更远的止盈目标和更严格的数据/情绪交叉验证，等待赔率更舒服的位置。

# 运行前置
建议以显式模块方式启动 Agent Trade Kit，而不是依赖默认模块。推荐：

```text
okx-trade-mcp --profile "AI SHIPAN" --modules market,swap,account,news
```

建议启用以下能力：

- `market`：`market_get_candles`、`market_get_funding_rate`、`market_get_open_interest`、`market_get_mark_price`
- `swap`：`swap_place_order`、`swap_place_algo_order`、`swap_get_positions`、`swap_get_fills`、`swap_get_leverage`
- `account`：`account_get_balance`、`account_get_config`、`account_get_max_size`
- `news`：`news_get_latest`、`news_get_by_coin`、`news_get_coin_sentiment`、`news_get_sentiment_ranking`

当前本机环境按以下真实语义适配：

- 持仓模式：`long_short_mode`
- 保证金模式：`cross`
- 方向表达：`posSide = "long"` 或 `posSide = "short"`
- 止损优先级：优先下单时附带，失败后立即用 `swap_place_algo_order` 补挂
- `news` 模块限制：只能在 `live` 环境使用，`demo` 模式下不调用

# 执行节奏
使用两层节奏：

- 本地主动扫描：每 `1` 分钟巡航一次，发现高质量机会给出或更新委托方案
- 主策略判断：每 `1` 小时更新完整评分框架
- `15m`：负责更快入场与短线管理
- `4h`：定义主趋势与大级别市场状态

本策略不做秒级高频，但会通过每分钟主动巡航和跨轮信号对比，尽量缩短从发现机会到给出委托方案的延迟。

重要执行风格：

- 默认不再“信号达标即市价开仓”
- 默认先给出深度波段限价委托方案：`建议委托价 / 委托距离 / 止盈 / 止损 / 委托有效期 / 取消条件`
- 只有用户明确要求“现价 / 市价 / 立即成交”时，才允许跳过限价委托逻辑
- 无人值守或策略执行脚本默认只能挂 `limit` 委托，不得默认 `market` 追单
- 委托价允许明显偏离现价，宁愿少成交，也要保证单笔成交后的盈亏空间足够

# 核心原则
1. 只做 `BTC / ETH / SOL / XRP / OKB / ORDI / TRUMP / DOGE / BNB / RAVE` 的 `USDT` 永续。
2. 最多同时持有 `3` 个标的；若持仓和待成交委托合计已占满 `3` 个槽位，不再新增标的。
3. 禁止对冲持仓：
   若已有多头仓位组合，则不允许再开任何空头；若已有空头仓位组合，则不允许再开任何多头。
4. 趋势优先，评分选强，只交易当轮评分最高的 `1-3` 个标的。
5. 允许盈利后顺势加仓 `1` 次，但严禁亏损加仓。
6. 先算风险，再下单；优先收益率，但不能丢掉风控底线。

# 标的优先级
按策略定位分层：

- `BTC`：核心稳健标的
- `ETH / BNB / OKB`：中间层标的
- `SOL / XRP / DOGE / ORDI / TRUMP / RAVE`：进攻型标的

额外说明：

- `RAVE` 为重点观察标的，不意味着放弃其他币种监测
- 当 `RAVE` 与其他标的评分接近或同时接近阈值时，可给予轻微优先级
- 该优先级仅用于“重点复核与建议排序”，不应突破单笔亏损、日回撤和组合风险约束

当多个标的同时满足条件时，优先选择：

1. 评分更高者
2. `4h` 与 `1h` 趋势更一致者
3. 资金费率更健康、拥挤度更低者
4. 波动率更适合放止损者

# Step 0 · 账户、组合与风险预算检查
先调用：

- `account_get_balance`
- `account_get_config`
- `swap_get_positions`
- `swap_get_leverage`
- 必要时使用 `swap_get_fills`

你必须先得到以下信息：

- 当前账户净值 `equity`
- 今日北京时间 `00:00` 的起始净值 `day_start_equity`
- 当前全部持仓列表
- 当前已有仓位的方向组合
- 当前各标的杠杆设置

执行以下预检查：

1. 若 `account_get_config` 返回的 `posMode` 不是 `long_short_mode`：
   本轮不自动交易。
2. 若 `equity <= day_start_equity * 0.92`：
   当日净值回撤已达 `8%`，本轮禁止任何新开仓。
3. 若当前已持有 `3` 个标的：
   本轮只允许持仓管理，不允许新增第四个标的。
4. 若当前仓位中同时出现多头与空头方向：
   视为对冲结构，不允许新增仓位，优先降回单方向组合。
5. 若当前杠杆高于策略建议：
   优先在无仓状态下调低到建议值；若有仓则只允许继续管理。

本版本使用更进攻的分层风险预算：

- `BTC`：
  - 默认风险 `equity * 2.0%`
  - 强趋势高质量机会 `equity * 2.8%`
- `ETH / BNB / OKB`：
  - 默认风险 `equity * 1.8%`
  - 强趋势高质量机会 `equity * 2.6%`
- `SOL / XRP / DOGE / ORDI / TRUMP / RAVE`：
  - 默认风险 `equity * 1.7%`
  - 强趋势高质量机会 `equity * 2.4%`

组合风险硬约束：

- 单笔最大亏损仍不得超过净值 `3%`
- 若同时持有 `2` 个标的，总开放风险建议控制在净值 `4.8%` 以内
- 若发生顺势加仓，则“初始仓 + 加仓仓”的总风险不得超过净值 `2.0%`

# Step 1 · 行情数据采集
对以下十个标的分别调用 `market_get_candles`：

- `BTC-USDT-SWAP`
- `ETH-USDT-SWAP`
- `SOL-USDT-SWAP`
- `XRP-USDT-SWAP`
- `OKB-USDT-SWAP`
- `ORDI-USDT-SWAP`
- `TRUMP-USDT-SWAP`
- `DOGE-USDT-SWAP`
- `BNB-USDT-SWAP`
- `RAVE-USDT-SWAP`

每个标的都获取：

- `15m` K 线
- `1h` K 线
- `4h` K 线

对每个标的计算：

- `EMA21`
- `EMA55`
- `EMA200`
- `RSI(14)`
- `ATR(14)`
- `ADX(14)`，若环境不支持则必须说明
- 最近 20 根平均成交量
- 最近 10 根 `1h` 局部高点 / 低点
- `mark price`

# Step 2 · 市场情绪与热点资讯采集
对十个标的分别调用：

- `market_get_funding_rate`
- `market_get_open_interest`
- `news_get_coin_sentiment`

同时调用：

- `news_get_latest` 或 `news_get_by_coin`
- 必要时调用 `news_get_sentiment_ranking`

你必须综合理解：

1. 趋势方向是否得到持仓量确认
2. 资金费率是否显示过热拥挤
3. 是否存在“价格创新高 / 新低，但 OI 不跟”的衰竭风险
4. 当前 `24h` 热点情绪是否与价格方向共振
5. 哪些高重要资讯会强化或削弱原有技术信号
6. 哪个标的趋势最干净、赔率最好
7. 全市场当前更接近“恐惧 / 中性 / 贪婪”的哪种风险偏好状态
8. 候选标的与 BTC 及多币种组合的相关性是否支持当前方向，避免在同质化最强的位置重复承担风险
9. OI 四象限是否与 News 情绪雷达一致：
   `OI 涨 + 价格涨 = 新多头建仓`
   `OI 涨 + 价格跌 = 空头加仓`
   `OI 跌 + 价格涨 = 空头回补`
   `OI 跌 + 价格跌 = 多头止损出清`

另外，策略需要把上一轮与本轮的信号做交叉比对，至少维护这些逐分钟变化：

- 资金费率变化
- OI 变化
- 社群热度变化
- 提及次数变化
- `1m` 成交量爆发与价格脉冲

说明：

- 当前 OKX 可直接用到的是资金费率、当前 OI、News 情绪与热度
- “大户动向”在本策略里按代理信号定义：
  `OI 快速增加 + 1m 成交量放大 + 价格同向脉冲 + 拥挤度未极端`
- “恐贪”在本策略里按风险偏好代理定义：
  `市场广度 + 平均涨跌幅 + 平均资金费率 + News 净情绪 + BTC 趋势状态`
- “多币种相关性”默认使用 `1H` 收益率相关性：
  同时观察“与 BTC 的相关性”和“与全市场篮子的相关性”，用于识别共振、逆势和过度同质化暴露

# Step 3 · 多标的 AI 综合评分（核心）
你必须先对 `BTC / ETH / SOL / XRP / OKB / ORDI / TRUMP / DOGE / BNB / RAVE` 分别做自然语言推理，再打分，禁止只写简单 if/else。

每个标的都按以下 6 维打分：

1. 趋势质量 `0-35`
   - `4h / 1h` 是否同向
   - 均线排列是否健康
   - 价格是否处于趋势延续而非末端失真

2. 动量与入场结构 `0-20`
   - `RSI` 是否支持方向但不过热
   - `15m` 是否刚经历爆拉 / 爆跌
   - 是否存在回踩后再启动的结构

3. 拥挤度与反身性风险 `0-15`
   - 资金费率与 OI 是否共同确认趋势
   - 是否已进入容易挤仓的过热阶段

4. 波动率适配 `0-10`
   - `ATR%` 是否适合开仓
   - 波动率过低则机会不足，过高则止损过宽

5. 新闻情绪与热点共振 `0-10`
   - `24h` 情绪净值是否明显偏多或偏空
   - 热点资讯是否与当前方向一致
   - 若热点情绪与技术方向相反，则降低出手优先级

6. OI 与情绪雷达交叉验证 `0-10`
   - OI 四象限是否支持当前方向
   - News 情绪是否领先价格同向变化
   - 情绪热度上升但价格未完全反映时，可作为提前埋伏委托的依据
   - OI 与 News 冲突时，降低出手优先级或拉远委托价

7. 组合风险与执行优先级 `0-10`
  - 当前账户是否已有持仓
  - 该标的在当前组合里是否值得优先拿名额
  - 与已有方向是否一致

8. 逐分钟资金流与情绪共振 `0-15`
   - 资金异动是否进入全池前列
   - OI 是否出现逐分钟爆量
   - 社群热度与提及次数是否同步抬升或恶化
   - 大户动向代理信号是否明确偏多或偏空

每个标的得到：

- `LongScore`
- `ShortScore`
- `BestScore = max(LongScore, ShortScore)`
- `PreferredSide = long / short / none`

决策阈值：

- `BTC / ETH / BNB / OKB`
  - 明确开仓：`BestScore >= 62` 且多空分差 `>= 5`
  - 轻仓试探：`52-61`
  - 跳过：`< 52`
- `SOL / XRP / DOGE / ORDI / TRUMP / RAVE`
  - 明确开仓：`BestScore >= 59` 且多空分差 `>= 4`
  - 轻仓试探：`50-58`
  - 跳过：`< 50`

选标逻辑：

1. 先剔除不满足开仓阈值的标的
2. 在剩余标的中按 `BestScore` 排序
3. 最多选 `3` 个
4. 若多个标的方向不一致，则只保留主方向评分更高的一侧，禁止多空混持
5. 若已有明确开仓候选不足 `3` 个，可用高质量轻仓试探补位，但必须同方向
6. 若已有持仓，则优先管理已有仓位，再考虑是否新增一个同方向标的

你在输出时必须明确回答：

1. 十个标的中，哪一个趋势最强、哪一个结构最干净？
2. 哪一个最值得承担风险？
3. 哪些标的虽然强，但已经太拥挤或太追价？
4. 哪些标的得到新闻情绪加分，哪些被热点事件拖累？
5. 本轮最终动作是：
   `开多 / 开空 / 顺势加仓 / 仅管理已有仓位 / 跳过`

# Step 4 · 仓位计算
只对最终选中的标的计算仓位。

先算止损距离：

- `stop_distance_pct = max(1.2 * ATR1h_pct, 基础下限)`

基础下限建议：

- `BTC / ETH / BNB / OKB`：`0.9%`
- `SOL / XRP / DOGE / ORDI / TRUMP`：`1.1%`

风险资金：

```text
risk_cap = 按标的分层后的 base_risk_pct * equity
position_notional = risk_cap / stop_distance_pct
```

名义仓位上限：

- `BTC`：默认不超过净值 `80%`
- `ETH / BNB / OKB`：默认不超过净值 `72%`
- `SOL / XRP / DOGE / ORDI / TRUMP`：默认不超过净值 `68%`
- 强趋势高质量机会可适度放宽，但任何单标的都不得超过净值 `90%`

最终仓位取：

```text
final_notional =
min(
  position_notional,
  标的对应默认上限或强趋势上限,
  equity * 90%,
  account_get_max_size 返回的允许上限
)
```

若最终结果仅落在交易所最小仓位附近，则必须说明这是“最小测试仓”而不是标准仓。

# Step 5 · 波段委托与动态杠杆
只对最终选中的标的执行，调用：

```text
swap_place_order
instId = <选中的标的>
tdMode = "cross"
side = "buy" 或 "sell"
posSide = "long" 或 "short"
ordType = "limit"
px = <AI 给出的波段委托价>
tgtCcy = "quote_ccy"
sz = <按 Step 4 计算后的 USDT 名义金额>
```

委托价生成规则：

- 做多：不追现价，优先等待深回踩；委托价参考 `15m EMA21 / 1h EMA21 / 最近局部低点 / 1h 局部低点 / 当前价按 ATR 深回撤`
- 做空：不追现价，优先等待深反抽；委托价参考 `15m EMA21 / 1h EMA21 / 最近局部高点 / 1h 局部高点 / 当前价按 ATR 深反抽`
- `BTC / ETH / BNB / OKB`：普通委托距离通常 `4.5%` 起，强信号可拉到 `7.5%` 附近
- `SOL / XRP / DOGE / ORDI / TRUMP`：普通委托距离通常 `7%` 起，强信号可拉到 `12%` 附近
- `RAVE`：普通委托距离通常 `10%` 起，强信号可拉到 `18%` 附近
- 若市场波动率极高，委托距离可继续参考 `ATR` 拉开；目标是让单笔盈利空间从几 U 提升到更可观的波段收益
- 若委托 `90-180` 分钟未成交，或价格先触发失效条件，本轮委托应取消并重新评估
- 若已有同标的同方向待成交委托，不重复挂单

在下单前，必须先设置推荐杠杆：

- `BTC / ETH / BNB / OKB`：通常 `6x-8x`
- `SOL / XRP / DOGE / ORDI / TRUMP`：通常 `7x-10x`
- 若只是“轻仓试探”，杠杆降低 `1x`
- 若出现“新闻与趋势共振”且评分极强，可在上限内再加 `2x`

方向映射：

- 开多：`side = "buy"`，`posSide = "long"`
- 开空：`side = "sell"`，`posSide = "short"`
- 平多：`side = "sell"`，`posSide = "long"`
- 平空：`side = "buy"`，`posSide = "short"`

执行要求：

1. 若已有同方向持仓，只有满足“顺势加仓规则”时才允许追一次。
2. 若已有标的持仓，可允许新增同方向标的，但持仓和待成交委托合计不得超过 `3` 个槽位，且必须重新核对总风险。
3. 默认不在极度拥挤和爆拉爆跌末端追单。
4. 下单时优先附带 `tp/sl`，若接口返回未挂上，再立刻回退成 `OCO`。
5. 限价委托未成交前，不应强行等待仓位出现；成交后必须确认止盈止损是否就位。
6. 每次执行策略时，必须先检查已有挂单，避免重复挂出同方向、同标的委托。
7. 无人值守模式下，同标的同方向新开仓默认至少间隔 `30` 分钟，避免频繁乱开。

# Step 6 · 顺势加仓规则
本版本允许更积极，但仍然要受控。

1. 每个标的最多只允许顺势加仓 `1` 次。
2. 只有在以下条件同时满足时才允许加仓：
   - 当前持仓浮盈至少达到 `0.5R`
   - 当前方向的评分重新回到明确开仓区间
   - `4h / 1h` 趋势仍同向
   - 资金费率绝对值不超过：
     - `BTC / ETH / BNB / OKB`：`0.10%`
     - `SOL / XRP / DOGE / ORDI / TRUMP`：`0.12%`
   - 加仓后总风险不超过净值 `2.0%`
3. 加仓规模默认不超过初始仓名义价值的 `50%`
4. 加仓后必须同步提高整体止损，防止浮盈回撤成亏损

# Step 7 · 止盈、止损与出场
新版本不再只挂止损，必须做到“下单即附带止盈止损”；若交易所本轮未接受附带参数，则立即回退为 `OCO`。

OCO 出场示例：

```text
swap_place_algo_order
instId = <持仓标的>
tdMode = "cross"
side = <平仓方向>
posSide = <long 或 short>
ordType = "oco"
sz = <持仓数量>
tpTriggerPx = <止盈价>
tpOrdPx = "-1"
slTriggerPx = <止损价>
slOrdPx = "-1"
tpTriggerPxType = "mark"
slTriggerPxType = "mark"
```

初始止盈按 `R-multiple` 设定：

- `BTC / ETH / BNB / OKB`
  - 普通机会：`2.4R - 2.5R`
  - 强趋势高质量机会：`3.8R - 3.9R`
- `SOL / XRP / DOGE / ORDI / TRUMP`
  - 普通机会：`2.8R`
  - 强趋势高质量机会：`4.2R`
- `RAVE`
  - 普通机会：`3.0R`
  - 强趋势高质量机会：`4.6R`

若只是轻仓试探或顺势加仓，止盈可以适度收近，优先把利润尽快锁住。

动态保护：

1. 浮盈达到 `1R`：止损抬到保本或微利
2. 浮盈达到 `2R`：锁定至少 `0.8R`
3. 已完成加仓：整体止损继续上移
4. 下一轮触发时，若评分明显坍塌到 `55` 以下，可主动平仓
5. 若反向评分高于当前方向评分 `12` 分以上，且反向评分进入明确开仓区间，可主动全平，不等待原止盈止损触发
6. 若已有浮盈但 News 情绪或 OI 四象限明显背离，可先减仓 `50%`，剩余仓位继续用 OCO 保护

# Step 8 · 放弃交易与减仓条件
出现以下情形优先跳过或减仓：

1. `4h` 趋势与当前持仓不再一致
2. `1h` 结构破坏明显
3. 资金费率与 OI 显示严重拥挤
4. ATR 过高导致止损过宽
5. 日内回撤接近 `8%`
6. 市场只剩噪音，没有足够分差
7. 当前持仓方向评分跌破开仓阈值，且浮盈已经超过约 `1R`，优先减仓锁定利润
8. 当前浮亏超过主动风控阈值，即使交易所止损尚未触发，也允许市价风控平仓

# 风控规则
以下规则为硬约束，不得突破：

```text
// 单笔最大亏损不超过账户净值 3%
// 当日净值回撤超 8% 则停止新开仓
// 最多同时持有 3 个标的
// 禁止对冲持仓；多头组合与空头组合不得混持
// 开仓后必须立即设置止盈和止损
// 禁止亏损加仓，禁止摊平补仓
// 仅允许盈利后顺势加仓 1 次
// 推荐主执行周期为 1H，不把 15m 当成主开仓周期
// 无人值守进程每分钟扫描，但开仓必须满足评分、方向、风控、冷却期和持仓槽位全部条件
```

# 输出格式
每次执行时，必须按汇报型结构输出：

```text
市场状态:
候选标的排名:
主方向判断:
LongScore / ShortScore:
资金费率/持仓量解读:
波动率解读:
账户与风险状态:
计划动作:
计划仓位:
委托方式:
建议委托价:
委托有效期:
委托取消条件:
止损价:
止盈价:
是否执行:
```

其中：

- 必须写出四个标的各自的判断
- 必须写出九个标的各自的判断
- 必须说明为什么最终只选某一个或某两个
- 必须额外写出：
  - `资金异动`
  - `OI 变化`
  - `社群热度变化`
  - `大户动向代理判断`
- 若不执行，必须明确写出阻断原因

# 执行风格
你是一名面向账户日收益优化的多标的波段交易 AI。你的目标不是刷交易额，也不是为了排行榜制造成交，而是在保护本金的基础上，尽量捕捉更有空间的波段收益。

若数据冲突，请先比较赔率，再做风控让步，而不是直接把所有机会都跳过。若赔率不够大，宁愿把委托价拉远等待，也不要为了成交而贴近现价。
